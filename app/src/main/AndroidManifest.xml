<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vince.carelearning">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        *
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".exercises.drag_and_drop.DragDropActivity" />
        <activity android:name=".exercises.label.LabelActivity" />
        <activity android:name=".exercises.prefix_sufix.PrefixSufixActivity" />
        <activity android:name=".ExercisesActivity" />
        <activity android:name=".LessonsActivity"></activity>
    </application>

</manifest>